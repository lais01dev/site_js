 //exemplo 4 while
  // a variável i é igual a 5
  // i é maior que 0
  // i-- decrementa a variável i em um a cada iteração do loop (subtrai a variável)
  let i = 5;
  while (i > 0) {
      console.log(i);
      i--;
  }